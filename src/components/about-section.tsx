import { NAV_ITEMS } from "@/consts/nav-items"

export function AboutSection() {
  return (
    <div id={NAV_ITEMS[0]} className="panel bg-accent/10 min-h-screen">
      <h1>About</h1>
    </div>
  )
}
